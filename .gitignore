node_modules/
test-results/
playwright-report/
playwright/.cache/
.DS_Store
.vercel
.env*

# Written by scripts/ship.ts at deploy time and regenerated every deploy;
# js/proof.js compares it against data/perf-budget.json's codeCommit.
data/release.json

# Scratch harnesses from the parallel build. Departments write these to the
# repo root because Playwright only resolves @playwright/test from the project,
# not from a temp dir. They are throwaway and must never be committed.
__*.mjs
check-*.mjs
.deliver.mjs
*.tmp.mjs

# Agent tooling installed by the Resend integration and local Claude sessions.
.agents/
.claude/
skills-lock.json

# Transition candidates: the mp4s are for judging, not for shipping.
# Regenerate with: bun lab/transitions/capture.mjs http://localhost:8340
lab/transitions/video/
