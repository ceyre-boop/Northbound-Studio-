{
  "name": "northbound-studio",
  "private": true,
  "type": "module",
  "scripts": {
    "perf": "bun scripts/perf.mjs",
    "perf:emit": "bun scripts/perf.mjs --emit",
    "test": "bunx playwright test",
    "serve": "python3 -m http.server 8099"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
